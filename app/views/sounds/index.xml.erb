<div id='soundboardify' style='visibility:hidden;'>
  <h4>soundboardify</h4>
  <% @sounds.each do |sound| %>
    <div class="sound-wrap">
      <img src="<%= sound.image_url %>" class="sound" title="<%= sound.name %>" data-url="<%= sound.url %>" />
      <span class="sound-name">
        <%= sound.name %>
      </span>
    </div>
  <% end %>
</div>