<div id='soundboardify' style='visibility:hidden;'>
  <h4>soundboardify</h4>
  <% @sounds.each do |sound| %>
    <div class="sound">
      <img src="<%= sound.image_url %>" class="sound-img" title="<%= sound.name %>" data-url="<%= sound.url %>" />
      <span class="sound-title">
        <%= sound.name %>
      </span>
    </div>
  <% end %>
</div>